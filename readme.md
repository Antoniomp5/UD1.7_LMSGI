Este readme está destinado a ser un comentario para complementar la tarea,
ya que JSON no soporta los comentarios.

Esta tarea es de conversión de un XMl a un JSON usando las herramientas dadas 
anteriormente, sumado a que se hizo cambios para que existiera un valor 
de tipo booleano siendo en la etiqueta "enOferta".

Se hizo en base al caso dos del PDF dado, ya que se repeitan varias etiquetas.
